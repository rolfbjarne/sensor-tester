name: Heat sensor
on: [ issues, pull_request, issue_comment, pull_request_review, pull_request_review_comment ]

# This action only need a single permission in order to autoformat the code.
permissions:
  contents: read

jobs:
  detect-heat:
    name: Hot Topic Detector
    runs-on: ubuntu-latest
    - steps:
      - uses: jonathanpeppers/inclusive-heat-sensor/.github/workflows/comments.yml@b29f03896f56ce39d9dc58a49d61f45042951df7
